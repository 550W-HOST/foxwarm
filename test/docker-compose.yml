#!/bin/bash
# Main test environment for foxwarm
# For multi-test-environment setup, copy this directory and modify:
# - name (below)
# - service name (foxwarm-test)
# - ports (3002:3001 → 3003:3001, etc.)

name: foxwarm-toolscript-managed-session-test

services:
  foxwarm-toolscript-managed-session:
    build:
      context: ..
      dockerfile: Dockerfile
    container_name: foxwarm-toolscript-managed-session-test
    ports:
      - "3004:3001"
    volumes:
      - ..:/app
      - /home/ldmbot/temp-worktrees/foxwarm-toolscript-phase1/node_modules:/app/node_modules
      - ./state:/app/state
      - ./agents:/app/agents
      - ./.env:/app/.env:ro
    restart: unless-stopped
    stop_grace_period: 5s
    command: ["node", "lib/index.js"]
    deploy:
      resources:
        limits:
          memory: 2G
